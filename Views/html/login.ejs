<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>Sign Page</title>
</head>
<body>
    <header>
        <br>
        <a href="/home">
            <img class="logo-img" src="../media/logo.svg" alt="logo image" width="80px">
            <h2>Book Smart</h2>
        </a><br>
    </header><hr>
    <main>
        <div class="signin-section">
            <h1>Sign in </h1>
            <p>Free ebooks are waiting for you.</p>

             <!--displaying errors if any-->
             <% if(typeof msg != 'undefined') {%>
                <p class="error"><%= msg %></p>
            <% } %>

            <% if(typeof err != 'undefined') {%>
                <% for (var i = 0; i < err.length; i++) { %>
                    <p class="error"><%= err[i].msg %></p>
                <% } %>
            <% } %>

            <form action="/login/user" method="post">
                <input type="text" id="email" name="email" required placeholder="Email"><br><br>            
                <input type="password" id="pass" name="pass" required placeholder="Password"><br><br>
            
                <button type="submit" class="submit-btn">Sign in</button>
            </form>
        </div>	
        <div class="signup-link">
            <p>Don't have an account?</p>
            <a href="/signup" class="signup-btn"></href><button >Sign Up</button></a>
        </div>
    </main>
    <footer>
        
    </footer>
</body>

<style>
*{
    margin: 0;
    padding: 0;
}
html{
    width:100%;
    height: auto;
    text-align: center;
}
body{
    max-width: 1000px;
    margin: auto;
}
header a{
    text-decoration: none;
    color: #000;
}

.signin-section{
    width: 60%;
    margin: auto;   
    margin-top: 4vh;
    display: border-box;
    padding: 2vh 4vw;
    border-radius: 20px;
    box-shadow: rgba(100, 100, 111, 0.2) 0px 7px 29px 0px;
}
.signup-link{
    width: 60%;
    margin: auto;   
    margin-top: 4vh;
}
h1{
    font-size: 2.4rem;
    text-align: left;
    padding-left: 18%;
}
.signin-section p{
    font-size: 1.4rem;
    text-align: left;
    padding-left: 18%;
    padding-top:  0.2em;
    padding-bottom: 1em;
}
input{
    width: 60%;
    height: fit-content;
    padding: 0.8rem 2%;
    font-size: 1.2rem;
    border: 1px rgb(225, 220, 220) solid;
    border-radius: 5px;
}
.submit-btn{
    width: 64%;
    height: fit-content;
    font-size: 1.4rem;
    padding: 0.6rem 0.6em;
    border: none;
    border-radius: 22px;
    background-color: #F2A007;
    color: #fff;
    margin-bottom: 5vh;
    transition: all 0.2s ease-out;
}.submit-btn:hover{
    cursor: pointer;
    background-color: #f2a007a0;
}
.signup-link p{
    padding: 1.5vh;
    font-size: 1.2rem;
}
.signup-btn button{
    width: 64%;
    height: fit-content;
    font-size: 1.4rem;
    padding: 0.5rem 0.5em;
    border: 2px solid rgba(0, 0, 0, 0.878);
    background-color: transparent;
    border-radius: 22px;
    margin-bottom: 5vh;
    transition: all 0.2s ease-out;
}.signup-btn button:hover{
    cursor: pointer;
    border-color:#F2A007;
    background-color: #F2A007;
    color: #fff;
}.error{
       color: #e21a1a;
    }

</style>
</html>